# Copy this file to .env and fill in your local MySQL credentials
DB_HOST=localhost
DB_USER=root
DB_PASS=your_safe_password
DB_NAME=blood
DB_PORT=3306
PORT=3001

# IMPORTANT: For development only. This backend exposes an endpoint that executes
# arbitrary SQL queries. Do not run this in production or expose it to untrusted networks.
